
  # Filme site builder

  This is a code bundle for Filme site builder. The original project is available at https://www.figma.com/design/gyBff0oJlvsI2idCXfU0WT/Filme-site-builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  